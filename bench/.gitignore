# Generated benchmark output — not committed
*.json
!results/*.json

# Old stress test scripts (kept locally, not shipped)
stress_test*.js
bench_agent.js

# Temp working directories left over from interrupted runs
bench-*-tmp/
